import React from 'react';

const Rank = ({ name, entries }) => (

    <div>
        <h2>
            Hi 
            {name}
            , your current is: 
            {entries}
            
        </h2>
    </div>

);

export default Rank;
